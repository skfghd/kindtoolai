# Firebase 환경 변수 설정 예제
# 이 파일을 참고하여 firebase functions:config:set 명령으로 환경 변수를 설정하세요

# Google Analytics 4 측정 ID (선택사항)
# firebase functions:config:set analytics.measurement_id="G-XXXXXXXXXX"

# Google AdSense 퍼블리셔 ID (수익화용)
# firebase functions:config:set adsense.publisher_id="ca-pub-XXXXXXXXXXXXXXXX"

# 관리자 패스워드 (기본값: new1234!)
# firebase functions:config:set admin.password="your_admin_password_here"

# 세션 시크릿
# firebase functions:config:set session.secret="your_random_session_secret_here"

# Firebase 프로젝트 설정
# 1. Firebase Console에서 새 프로젝트 생성
# 2. Firestore Database 활성화
# 3. Firebase Hosting 활성화
# 4. Firebase Functions 활성화

# 환경 변수 설정 명령어 예시:
# firebase functions:config:set analytics.measurement_id="G-XXXXXXXXXX" adsense.publisher_id="ca-pub-XXXXXXXXXXXXXXXX" admin.password="new1234!" session.secret="your_secret_here"

# 현재 설정 확인:
# firebase functions:config:get

# 배포 명령어:
# npm run build:firebase  # 빌드
# firebase deploy         # 전체 배포
# firebase deploy --only hosting  # 호스팅만 배포
# firebase deploy --only functions  # 함수만 배포